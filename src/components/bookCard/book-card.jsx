import { useEffect, useState } from 'react';

export const BookCard = () => {
  const [ bookList, setBookList ] = useState([]);

  useEffect(() => {
    setBookList([]);
  });

  return (
    <>
      <div />
    </>
  );
};
